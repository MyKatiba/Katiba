package com.katiba.app.data.api

/**
 * iOS implementation of getGeminiApiKey.
 * For iOS, the API key should be set via environment variable or xcconfig.
 * For now, we provide a placeholder that should be replaced with the actual key at build time.
 */
actual fun getGeminiApiKey(): String = ""

